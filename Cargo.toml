[workspace]
resolver = "2"
members = ["weft-core", "weft-installer", "weft-launcher", "weft-windows"]

[workspace.package]
edition = "2021"
license = "MIT"

[profile.release]
lto = "thin"
strip = true
